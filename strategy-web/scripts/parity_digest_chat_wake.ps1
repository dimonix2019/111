# DEPRECATED — duplicate of hourly chat wake.
# Use parity_recon_agent_loop.ps1 (AGENT_LOOP_TICK_parity_recon) instead.
# Trade open/close: parity_trade_chat_wake.ps1 (AGENT_LOOP_WAKE_parity_trade).
# Exits immediately so accidental starts do not spam the chat.

Write-Output 'DEPRECATED: use parity_recon_agent_loop.ps1 + parity_trade_chat_wake.ps1 (no Cursor Automation). Exiting.'
exit 0
